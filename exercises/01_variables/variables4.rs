// TODO: Fix the compiler error.
fn main() {
    let mut x = 3;  // by default all variables are unmutable , we need to add mut keyword to make it mutable!!
    println!("Number {x}");

    x = 5; // Don't change this line
    println!("Number {x}");
}
