fn main() {
    // TODO: Change the line below to fix the compiler error.
    let x = 0; //we have to intialise a variable with value in rust!

    if x == 10 {
        println!("x is ten!");
    } else {
        println!("x is not ten!");
    }
}
